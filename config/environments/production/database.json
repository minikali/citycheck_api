{
  "defaultConnection": "default",
  "connections": {
    "default": {
      "connector": "bookshelf",
      "settings": {
        "client": "mysql",
        "uri": "mysql://hzzhllh158:Citycheck31@hzzhllh158.mysql.db:27017/hzzhllh158",
        "host": "${process.env.DATABASE_HOST || 'hzzhllh158.mysql.db'}",
        "port": "${process.env.DATABASE_PORT || 27017}",
        "database": "${process.env.DATABASE_NAME || 'hzzhllh158'}",
        "username": "${process.env.DATABASE_USERNAME || 'hzzhllh158'}",
        "password": "${process.env.DATABASE_PASSWORD || 'Citycheck31'}"
      },
      "options": {
        "debug": true,
        "pool": {
          "min": 0,
          "max": 10,
          "idleTimeoutMillis": 60000,
          "createTimeoutMillis": 60000,
          "acquireTimeoutMillis": 60000
        }
      }
    }
  }
}
