{
  "routes": [
    {
      "method": "GET",
      "path": "/",
      "handler": "SocialLinks.index",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/submitSocial",
      "handler": "SocialLinks.submitSocial",
      "config": {
        "policies": []
      }
    }
  ]
}
